@model Web.Models.SearchFilterModel
@{
    const string formId = "GetCurrencyForm";
}
@{
    Html.BeginForm(
        actionName: "",
        controllerName: "",
        routeValues: new
        {
            @area = ""
        },
        method: FormMethod.Get,
        antiforgery: true,
        htmlAttributes: new
        {
            @class = "container",
            @id = formId,
            @name = formId
        }
    );
        <fieldset form="@formId">
            <legend>
                Obtener información divisa
            </legend>

            <div class="row">
                <div class="col-md-6">
                    <div class="input-group input-group-lg mb-3">
                        @Html.LabelFor(
                            expression: model => model.Year,
                            htmlAttributes: new
                            {
                                @class = "input-group-text"
                            }
                        )

                        @Html.EditorFor(
                            expression: model => model.Year,
                            additionalViewData: new
                            {
                                htmlAttributes = new
                                {
                                    @class = "form-control"
                                }
                            }
                        )
                    </div>

                    @Html.ValidationMessageFor(expression: model => model.Year)
                </div>

                <div class="col-md-6">
                    <div class="input-group input-group-lg mb-3">
                        @Html.LabelFor(
                            expression: model => model.Month,
                            htmlAttributes: new
                            {
                                @class = "input-group-text"
                            }
                        )

                        @Html.EditorFor(
                            expression: model => model.Month,
                            additionalViewData: new
                            {
                                htmlAttributes = new
                                {
                                    @class = "form-control"
                                }
                            }
                        )
                    </div>

                    @Html.ValidationMessageFor(expression: model => model.Month)
                </div>
            </div>
        </fieldset>
    Html.EndForm();
}